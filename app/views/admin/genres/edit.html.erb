<div class="container">
  <h2 class="title">ジャンル編集</h2>
    <%= form_with model: [:admin, @genre] do |f| %>
      <div class="field">
        <%= f.label "ジャンル名" %>
        <%= f.text_field :name %>
      </div>
      <%= f.submit "変更を保存", class: "btn btn-success" %>
    <% end %>
</div>
